services:

  gc-front:
    image: coderlady/gc-front:0.0.1
    container_name: gc-front
    restart: always
    networks:
      - app-network

networks:
  app-network:
    external: true
    name: app-network
